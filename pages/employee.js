import Employee from "../components/pages/employee";

/**
 * This will render a page on the route https://unijob.app/employee
 * @param {object} props Properties we are passing into this employee page
 * @returns An instance of the employee page.
 */
export default function Employee(props)
{
    return <Employee {...props} />
}